
Comiler: **NVCC**
```bash
nvcc -o prog prog.cu
```
`-o`: output executable file will be called `prog` instead of `a.out`

![[CUDA compiler - nvcc.png]]

remember: dealing with GPU on NYU's HPC is different then dealing with multicore; so test it out before doing hw