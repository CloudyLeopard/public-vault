Single instruction, single data stream. A type in [[Flynn's Taxonomy]].

It is also known as the **classical von Neumann** system (consisting of a main [[Memory Access|memory]], a [[Processor|CPU]], and an interconnect between memory and CPU)

It executes a single instruction at a time, and in most cases, computes a single data value at a time.