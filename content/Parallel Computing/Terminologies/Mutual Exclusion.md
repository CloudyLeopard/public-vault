---
aliases:
  - lock
  - locks
---
No more than one thread executes [[Critical Section]] at any point in time. Must line up the threads so that they run critical section code one at a time

Enforced through **locks** **(mutex, semaphore, ...)**