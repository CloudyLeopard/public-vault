Note just computation, but computation AND communication AND other costs.

We want all the threads/processes to **finish at roughly the same time** - in other words, have the same "work". Otherwise, the faster thread/process will have to wait for others.