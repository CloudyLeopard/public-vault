Last class, we showed that we are moving from single core to multi core
Parallel machines are here to say

Looked at historical machine from the old days until we reached multi-core
 The trend is to have more core inside the chip, but there is another trend which is the course may not be a copy and paste from each other - there are heterogenous cores
 - different companies will have different cores with different characteristics
## How did the hardware evolve like that?
Different waves (generations of architectures). *What are the cores like*

### First Generation (1970s)
**Single cycle implementation**: a black box, gets an instruction, executes the instruction, gets an output; get next instruction, executes it, gets another output, etc.
![[first generation core.png]]

> [!important] Gigahertz
> What does it mean for a computer to have, let's say, 2 GHz?
> In any digital computer system, there is a small circuity called the **clock**. The clock generates an electric signal that goes down then up and etc.
> **Cycle**: The time between the first "up", then down, then the second "up"
> The higher the **frequency**, the higher the number of cycles; Higher frequency = more work to be done per second

> [!Question] I don't get this
> The "cycle" has to be up for as long as it needs.

But not instructions are created equally. How long will you make the cycle? It has to be the length of the slowest instructions among all the instructions that can be executed

This is **bad**
1. Inefficiency: long cycle = fewer frequency. We are making the frequency **as slow as the slowest instruction**, which means all the instructions will take as slow as the slow instruction.
2. Power Consumption: Sophisticated circuity: power hungry
3. Resource Waste: this circuit has a part that fetches instruction from the memory, a part that tries to decode it (cuz from the memory its 0s and 1s), a part that executes it, etc. So, when you have an instruction is being executed, *most of the circuit is being idle*

### Second Generation

![[Second generation core.png]]

Now, assume we have 4 instructions. In one cycle, the first instruction is fetched. In the second cycle, the "fetch" instruction will move forward, and the fetch part can fetch again. This is a type of **parallelism**: they are executing 5 instruction in parallel *without programmer interference*

![[second generation core pipeline.png]]

**Pipelining**:
- the hardware divided into *stages*
- **temporal parallelism**: temporal because they are parallel while each program is in a *different stage in time* (fetch/decode/issue/execute/commit)
- Number of stages increases with each generation

> [!Question]
After instruction finishes the "commit" stage, the best scenario is that every program gets committed. Which means...
Minimum **CPI** (cycles per instruction) = 1
*The lower this number the better. Remember, cycle is really a time frame. More cycle = more time. The lowest we can get is 1 cycle per instruction*

Note: different companies have different stages. Some companies may split it into 6, 8, 3, etc. And, the number of stages is usually a secret for each company. We are using 5 as a general case. 

Now, since each stage is smaller, we can have a higher frequency.

HOWEVER, *many cases CPI can become > 1*:
- What if the instruction is a conditional branch?
	- The fetch will not be able to know what instruction to get (since I will never know if A > or < B until the instruction (the if condition) reaches the execution phase)
	- Until that instruction reaches execution, *the fetch stage will not be able to fetch*
- Dependencies among instructions (i.e. an instruction must wait for the result of another)
	- Suppose you want to add `rax` to `rdx`, but both are dependent on two other instructions
Because of these, the optimal CPI=1 is rarely achieved.

> [!info] How severe is this conditional branch jump?
> VERY SEVERE. Every thing related to function call and objected oriented programs use, to some extent, jumps and conditional jumps. 

Most of the programs we run today have conditional jumps. As a result, the efficiency of the pipeline is bad. BUT, the idea of having a pipeline is good, because we add parallelism AND we didn't really add that much hardware
So, need to do something about *conditional jumps*

Also, look at the "Execute" stage: inside that, we have a lot of different parts
- addition and substraction of int
- multiplication and division of int
- .. of float
- blah blah blah
Which means whenever we are using a part, the rest is **idle**. 

> [!important] idle
> In computer system, being "idle" does not mean its turned off. It is still using power.


### Enhancements
1. Cache Memory
	1. Multi-level caches: level 1, level 2, level 3 cache
2. Virtual Memory
	1. TLB: a piece of hardware used to speed up virtual memory (which is used everywhere now)

### Third Generation

![[Third generation core.png]]

The Instruction stage will know which execution part to send to, and also makes sure dependency is satisfied

> [!danger] Rewatch recording
> Rewatch his part on this slide. I missed it in class (was chatting lol)

**ILP** (Instruction Level **Parallelism**)
**Spatial parallelism**
Executing several instructions at the same time is called **superscalar capability**.
Performance=severla isntructions per cycle (**IPC**)
**Speculative Execution** (prediction of branch direction) is introduced to make the best use of superscalar capability -> this can make some instructions execute **out-of-order**

Here, the measure of success is different - in one cycle, how many instructions finish? (This is IPC)

#### Conditional if-else: Branch Prediction
if there is a condtional if-else, what do i do? if i just jump, this will make the performance go down

This is a hot topic debate back then.
**Branch prediction**: an additional piece of hardware which has an algorithm that predicts if the condition is true or false. This is *also* under NDA.

How does this work?
- The first time it encounters a branch it does not make any prediction. Just see and wait.
- if you see this branch a second time (which happens, cuz for loops etc.). Then you see a program - if you see osmething liek True True False False, which means maybe the next is True (pairs)

This was a hot topic back then - there were branch prediction championships

Though this does lead to some vulnerability
The thing is, when you make a prediction then you go from D to E etc. But, you won't know if the prediction is correct or not
- if it is correct its ok
- But if it is not correct, you gotta erase a lot of stuff and restart etc. Which causes a lot of performance loss

This is why machine-learning branch prediction doesn't work, since even if it is very accurate, it is slow and power hungry - might as well have a lower accuracy but faster
- e.g. prediction in one cycle, and if you have a 4GHtz machine thats just 25 nanoseconds

Note: now all the cores have branch predictions

#### Next Change
If we allow instructions to be fetched from more than one thread, then maybe we can keep these pieces of hardware busier
- right now, we are getting all these pieces of instruction from one thread. What if we get instrutions for multiple threads?
- E.g. media player being written in multiple thread, and we get from both of them

### Fourth Generation (2000s)
![[fourth generation core.png]]
**This whole picture is ONE CORE**

Double (or triple r...) some resource sin the pipeilne to host severla programs *at the same time*. This allows better use of the execution resources

Getting instruction from more than one thread at a time, and they are all sharing a bunch of executing units. And, you will commit them all by itself.

Here, we have superscalar capabilities and pipelining, etc. **Starting from this generation, programmers have to do stuff**.
- run two different programs
- write a program that use multiple threads that can use both threads

Most of the *performance core* have **hyperthreading technology** (note, this is a marketing term)
- performance core have more execution units, so its faster, but it consumes more power.
- The efficiency core may not has as much execution units, have lower frequency but consumes less power.

## Some Definitions
An operating system "**process**": an instance of a computer program that is being executed.
- A process is an operating system process. Its not just a code to be executed.
- Its more like a box that contains the instruction to be executed (whether it is one thread, or multiple threads, etc.)
- It has information on what resources this program is using (e.g. what files, how much memory it is taking, billing information - if you are using aws google etc., ...)
- What is really being executed by the core is the **code itself**
Components of a process:
- the executable machine language program (the thread - single or multiple if parallel code)
- A block of memory
- Descriptors of resources the OS has allocated to the process
- Security information
- Information about the state of the process

We will learn about how to create multiple processes, how to create multiple threads in a process, and how to make processes talk to each other.

**Multitasking**: gives the illusion that a single processor system is running multiple programs simultaneously
- Each process takes turns running -> time slice
- **Context switching** - really its just running one program, switches to another, runs it, etc.
- But if it does this really quickly, it looks like its all done i parallel
- Few processes can run in parallel if the hardware is equipped with several cores. THIS PART - assigning which code goes to which core - is OUTSIDE OF THE PROGRAMMERS CONTROL
	- all you can do is design as many threads as you can (you want to create more threads than cores - if one thread on a core is doing something that is taking a long time, and the OS can remove it and put another waiting thread on it)

**Threading**: threads are **contained within processes**. the part of the code being executed
- allow programmers to divide their programs into (more or less) independent **tasks**
- The hope is that when one thread blocks because it is waiting on a resource (e.g. memory, from internet), another thread will have work to do and can run


## The Status Quo
we moved from single core to multicore - w saw this in last lecture
software folks gotta do stuff, and cant just rely on having new generatino of processors

Problem: not enough experience in parallel programming
- parallel programs of old days were restricted to some elite applications -> very few programmers, organizations, or governments can do this stuff
- Now, we need paralle programs for *many different applications*

![[software hardware community.png]]Software community tell their wishes to computer architecture, who design chips and gives them to people who create these chips and tell them what can or what can be done (add restrictions but give some capabilities), goes back to cmoptuer architecutre
Computer architecture then tells software community what are the new performance and what are the new restrictions etc.

The multicore software triad (how development looks like)
- Application performance
- software reliability
- Development time

## Conclusions: 
The hardware evolution, driven by moore's law, was geared toward two things:
- exploiting parallelism
- dealing with memory (latency, capacity)

# Basic Architecture: Advanced
Review: we looked at techniques to exploit **Instruction Level Parallelism**
- Pipelining
- Superscalar
	- Out-of-order execution
- Speculative execution (branch prediction)
- Simultaneous multithreading (aka Hyperthreading technology)
*all of these require very little, if at all, work from the side of the programmer*

![[computer technology improvement.png]]
On processor, that's moore's law.
- in 1997, IBM made deep blue, designed to play chess. It cost about $10 million at that time, and first computer program running on specialized hardware to beat world chess championship. - first time computer chess won against a championship
- Now, any free chess program on the phone will beat the current world chess championship *without giving them any chance lmao*
- this gives an idea on how much performance has improved.

> [!Question]
> not sure if this is right
> More memory = less trip to the disc
> 
> Virtual memory: using parts of the disc to complement the memory because memory cannot hold everything. If you look at design of motherboard, processor is connected to the memory NOT the disc.
> 
> If the memory si small, you have to keep movingn stuff from the memory to the disc. so if you have to keep the first stuff you will slow down things - disc is SLOW (1000 times slower than memory) - each time you are going to the disc you are slowing down a  lot

## Memory Wall
**memory access**: is still a big problem in parallel machines
**cache coherence**: also has large negative effect on performance (will discuss in next lecture)

> [!question] Story
> two threads executing two cores, each core have its own cache
> suppose we have written a parallel program - each thread executing on a different core. assume both read wants to read `x`, a global variable. They don't find it in the cache, gets cache miss, goes to memory, and `x` will go to both cache.
> now we will have THREE copies of `x`: two copies of `x` in the cache, one copy in the memory
> 
> Suppose now one thread is doing `x+2`, the other is doing `x+4` (ok i dont get this at all)
> but the general idea is cache coherence is used but will make things a lot slower.

### Memory Issue

![[memory wall.png]]
memory speed is increasing 7% per year - there is a BIG gap between processor and memory speed, and this gap is getting wider

Some people actually are calling this the Von Neumann bottleneck. Didn't really see this in the old days because CPU was really slow.

**most of the single core performance loss is on the memory system**

Most of the time memory is the issue, not the Big $O$  stuff

### GPU vs CPU Multi Core
GPU is also a processor. Both are chips.

**Flynn's Taxonomy**

|     | Single Data Stream | Multiple Data Stream |
| --- | ------------------ | -------------------- |
|     |                    |                      |
> [!Danger] Fill in from slides

![[Flynn's Taxonomy Diagram.png]]

MIMD: traditional multicore

GPUs target graphics, and the characteristics of that is to calculate the pixels on the screen, totally independent of each other, etc. **SIMD**

MISD: does not exist, and is there for completeness

Note: MIMD can execute SIMD programs, and so is the most generic. BUT, people design stuff specifically for SIMD even though MIMD exists. But, we don't do the same for MISD. Why?
- there are many applications that require SIMD's architecture. Also, all the Machine Learning applications also use SIMD (cuz they need matrix multiplication, and SIMD is the textbook definition of matrix multiplication)
- not as much application for MISD, and all of them can be done using MIMD.

SIMD
- Aside from GPU, this is also used in ****