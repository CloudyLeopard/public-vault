> [!warning] Listen to the first 10 minutes of the lecture again


## SIMD
Parallelism achieved by dividing data among the processors
Applies the same instruction (or group of instructions) to multiple data items at once
This is called **data parallelism**
Example:
- GPUs
- vector processors

> [!Info] Vector Processor
all the processors now have these "vectorized stuff", and *you do not initiate them yourselves*
The compiler knows the processors are vectorized, and will generate **assembly instructions for these vector operations**

![[SIMD example.png]]
Sequentially, this will be `x[0] += y[0]`, `x[1] += y[1]`, ...
But, with vectorized processors, this can all be calculated in *one cycle*

> [!Info] Example: What if we do not have as many execution units as data items?
> Divide the work and process iterative
> Example: 4 execution items and 15 data units
> ![[simd w vector calculation.png]]

> [!important] We will not be writing anything for SIMD
> This is because this is all done by the compiler


### SIMD Drawbacks
All execution units are required to execute the same instruction(s) or remain idle.
In classic design, they must also operate synchronously (i.e. together at the same time).
Efficient for large **data parallel** problems, but not other types of more complex parallel problems.

> [!info] Machine Learning
> SIMD actually became very important for machine learning, even if it is designed for graphics in mind at first (GPU). This is because this is really good for **matrix multiplication**, which is basically machine learning.
> 
> Example: Transformers can actually be done quite well by GPUs. Usually, when you compare this type of software, you are looking at the
> 1. cost
> 2. performance (quality of answer)
> 3. power efficiency (power it will consume
> 4. time (speed to give you answer after training)

> [!Question] Not sure about this
> **Question:** compare a GPU that is 2x more advanced vs using two GPUs.
> "Depends" on how parallelizable the problem is - meaning the program can be split up and does not need as much communication (which is costly)

### SIMD Example: Vector Processors
Processors execute instructions where **operands are vectors** instead of scalars
This needs
1. **vector registers**: capable of storing
...
> [!danger] fill in notes from presentation here


| Pros                                                                                                                                        | Cons                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| Fast                                                                                                                                        | They do not handle iregular data structures                                     |
| Easy to use                                                                                                                                 | Very finite limit to their ability to handle larger problems (scalability) [^3] |
| Vectorizing compilers are good at identifying code to exploit                                                                               |                                                                                 |
| Compilers also can provide information about code that cannot be vectorized - helps programmer re-evaluate code                             |                                                                                 |
| Makes the best use of **memory bandwidth**: bringing a bunch of values is better use for the bandwidth than bringing a value at a time [^1] |                                                                                 |
| Uses every item at the cache [^2]                                                                                                           |                                                                                 |
[^1]: Suppose you have to add two numbers, you have to bring them from memory to cache (register), then make the addition. Suppose we are looking at an int (32 bits). The width of the BUS connecting to the CPU is 64 bit. So, you are losing half of the BUS. But, if you can get a bunch of numbers at once, you are using all of the BUS bandwidth.
[^2]: ==not sure about this check presentation again==
[^3]:  machine has a finite # of vectorized execution units, and finite # of vectorized registers


## MIMD
Supports multiple simultaneous instruction streams operating on multiple data streams

Typically consist of a collection of fully independent processing units or cores, each of which has its own ==control unit== and its own ALU.

Example: multicore processors, multiprocessor systems
### Flynn's Classification
**Flynn's classification** is based on how instructions and data are used. See below for examples of them
#### Shared Memory System
Collection of **autonomous processors/cores** is **connected to a memory system** via an **interconnection network**

Each processor/core can access each memory location
The processors/cores usually communicate implicitly by accessing data shared in memory

![[shared memory system.png]]

> [!info] Example
> Suppose one CPU wants access `addr1`, and another CPU wants `addr2`. Assume we do not have cache. The two CPUs will *not* see the same **memory access delay**.
> 
> Answer: **Banks**. The first bank will contain addr 0-31, second bank will contain 32-63, etc. 
> - If two CPUs or cores access the same bank, one gotta wait for the other
> - If two CPUS or cores access two different banks, they will respond differently too because *the distance is different* (**non uniform access distance** i think)


#### Distributed Memory System
Collection (cluster) of nodes
- Connected by an **interconnection network**
![[distributed memory system 1.png]]

Every node can have one CPU (or multicore) and one memory.
For simplicity, in the picture above, each node has 1 CPU and 1 memory, and are interconnected *somehow*. Here, you can have processes running on different nodes and communicate with each other
- They **cannot** communicate by memory (they have different memories)
- They **communicate** by **sending messages**

> [!info] MPI
> The first programming language we will learn - for sending messages between nodes i believe.

## Summary
![[SIMD MIMD summary.png]]

Master Worker: one node is more important than the others
SMP (Symmetric Multi Processor): All nodes are the same

> [!info] Example: PS3 Processor
> There was one famous processor that is working in that "other thing" - and it went really viral and went into some gaming machine and then died. This is the cell processor that used to be in the Play Station 3. It died because it was *very hard to program*.
> - Think about writing a parallel program where not every core is equal in terms of performance, but also in importance (one is generic (master), others are specialized (9 workers))
> - So, one of the important thing in hardware design, is **progammability** - you want to design a program that is very easy to code

**Nodes are connected by *interconnection networks***

## Interconnection Networks
This affects performance of both distributed and shared memory systems
- Communication is *very expensive*, so this is important

Two categories: 
1. Interconnect for shared memory
2. Interconnect for distributed memory

> [!important] Why do we need to separate the two?
> Distributed is *more* complicated, and distribute *must be able to scale* (e.g. you can add more nodes to it like in a super computer), whereas in shared memory its all on a shared motherboard and its not like you can just make a mother board bigger

> [!info] Using Lasers?
> In general, the cores and memories are *electronic* devices. So, to use lasers you gotta add more onto the cores and memories so its like even slower


### Shared Memory Interconnects
Bus Interconnect
- A collection of **parallel communication wires** together with hardware that controls access to the bus
- Communication wires are shared by devices that are connected to it
> A bus is a broadcast medium. If one core puts a data on the bus, all cores will see it. So, you cannot have multiple cores put data onto the bus otherwise something will go wrong. You gotta add some stuff to it to control that only one entity is sending data onto the bus
- As the number of devices connected to the bus increase
	- contention for use fo the bus increase
	- communication becomes unreliable due to noise
	- Performance decrease
> What do you connect onto the bus? Core and memories.
> Problem: not all devices today use bus. 
> 1. This is because it is not scalable. If several cache needs to send to the global memory, and if number of cores increase it will make the performance go down very quickly.
> 2. The electric engineers said that if you increase connection to bus it will make electronic noise, which means transmitting data will be noisy and get distorted


**Switched Interconnect**
- uses **switches** to control the **routing** of data among the connected devices
- Those switches are conected by **wire** forming network of some **topology**
- Example: crossbar
	- allows simultaneous communication among different devices
	- faster than buses
	- but the cost of the switches and links is relatively high

Topology is a subfield of mathematics - a study of shapes or something and you gotta look at shapes to compare distance between points etc. These are all mathematical optimization so they bring in mathematics

![[switched interconnect example - crossbar.png]]

> [!danger] do a bit more studying on this part


### Distributed Memory Interconnects
There are two types
1. Direct Interconnect: each **switch** is *directly* connected to a processor-memory pair, and the switches are connected to each other
2. Indirect interconnect: 
> [!warning] missing notes


#### Direct Interconnect
![[direct interconnect example.png]]
a. ring is good cuz P1 can send to P2 and P3 can send to P1 - could be even better than BUS

#### Indirect Interconnect
![[distributed memory systemcrossbar interconnect.png]]
> [!info] more in textbook


### Definitions Related to Interconnection Networks
> [!warning] missing notes

Assume this is one wire, with a source and destination. A source needs to send 0.5 byte (4 bites) to the destination
1. the source will send 1 bit, and put the next bit onto the line

![[latency and bandwith example.png]]

latency is the time it takes from 1 bit to start from source and reach destination
bandwidth: how many bites are received at the destination once per cycle (with one wire, bandwidth = 1)

$$ \text{Message transmission time} = l + n/b$$
- l = latency (seconds)
- n = length of message (bytes)
- b = bandwidth (bytes per second)

## Cache Coherence

> [!important] Cache Design
> Between the processor/core and the memory modules, there is one or more levels of cache
> 
> In most of the design, every core has a level 1 core (and small, because it has to be fast).
> Then, we have level 2 cache - still independent to each core
> And a level 3 cache which is shared by everybody
> 
> As we go down in level, we dont care as much about performance but *we want to increase the hit rate*. Level 3 is the last line of defense, and if that misses we go to memory and remember *that is SLOW*.
> 
> This is why usually in books they draw caches as a triangle

by introducing caches, we are actually introducing another challenge.
Suppose we have two cores, each core has a cache, and a shared memory. Suppose each one of these two cores is running a thread, and both threads needs to access `x`, a global variable. Suppose `x` is not in both cache.
- `x` will be brought into both cache. That means we will have **3** copies of the cache (1 in original memory, 2 copies in each cache)
- As soon as one of the threads try to update `x`, there is a problem - we have TWO MORE COPIES OF THE `x`. Even worse, what if both two threads are trying to update the same `x`? (and this is not uncommon with threads - program becomes unpredictable)
- 


Programmers have no controls over caches and when they get updated. But programmers *can write cache friendly code*

We will pick up from here next time - the **coherence problem**.