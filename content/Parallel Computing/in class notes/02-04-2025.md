## More on Caches Coherence
**Between the processor/core and the memory modules, there is one or more levels of caches**

Regardless of which company you are buying multicore from, each company has a level 1 cache.
- L1 cache *must* be **fast**, because it needs to be as close to the speed of the CPU (which is really fast) as possible

L3 cache: much bigger in size, and also higher associativity. It is much slower than L1 and L2
- there is only **one** L3 cache. this is because this is the last line of defense - if we get a cache miss at L3, we have to go to the memory which is a *very* lengthy trip. To reduce this, we have to reduce the hit rate of L3..
- So, in all the companyes, L3 is shared.

L2 cache: the main point of difference between the company. But now the trend is that every core has its own L2 cache

In essence: L1 must be fast (to match CPU), L3 must be big (to reduce cache miss), and L2 is dependent on company choice


one of the main reasons to drain a memory are the caches (processors right now)

WIith the processors back to the image, *we have a big challenge*

Programmers have no control over caches and when they get updated, *but programmers can write cache friendly code*

![[cache coherence example.png]]
When we say "core", we are referring to the thread thats running the code (i think)

we have global variable, `x=2`:

| Time | Core 0       | Core 1       | Comments                                                                                      |
| ---- | ------------ | ------------ | --------------------------------------------------------------------------------------------- |
| 0    | `y0 = x`     | `y1 = 3*x`   | After time 0, we have **3** copies of `x` <br>(cache 0, cache 1, and original copy in memory) |
| 1    | `x = 7`      | No `x` usage |                                                                                               |
| 2    | No `x` usage | `z1 = 4*x`   |                                                                                               |
Problem: after time 2, `y0 = 2`. But, **what is `z1`?**
- we have *three* copies of `x`

> [!warning]
> Such a situation is a **big mess** and must not happen. It will lead to buggy code. *But*, we still need shared variables (otherwise how do we parallelize stuff at all? why do we need shared memory at all?)

Answer: **cache coherence** protocol - a *hardware* side solution to the problem

cache appeared in 1995, and cache coherence came after that. When they first started, they had...
### Snooping Cache Coherence
1. The cores must share a bus
2. Any signal transmitted on the bus can be "seen" by all cores connected to the bus
3. When core 0, or any other core, updates the copy of `x` stored in its cache, it also broadcasts (the bus is a broadcast medium) this information across the bus
4. If another core is "snooping" the bus, it will see that `x` has been updated and it can mark its copy of `x` as invalid

(see cache coherence picture, the "interconnect" is the bus)

But this is no longer used, it is *not scalable*

> [!warning] Missing Notes
> Its not scalable because the bus is not scalable (tho idk why its connected)
> also there is two versions: the "update" and "invalidate" version apparently

### Directory Based Cache Coherence
Again, cache coherence is to make sure that *all copies are coherent* - all copies have the latest value. It's *not to ensure that there are no unnecessary copies of the cache*

> [!Info] Definition of a Cache Block/Blind/...
> If there is one block per set, there is a direct map set
> Two block per set: two way associative (or something i dont know)
> 
> Every block has a tag. if one of the block matches, that also will be used to go through that block and pick the required variable (idk if this is right).
> 
> A block is **a group of bytes**. In the current multicore, the cache block is 64 bytes (i think). When there is a cache miss, it will get an entire block of memory *i think* where the entire block that contains `x` is going to be moved into the cache. this is why if you get a cache miss at like `addr1`, and you try to then access `addr2`, you will find that `addr2` will also b in cache
> 

Note: cache is a hardware thing, it does not understand variables. So, really, from hardware pov, it doesn't rlly make sense to "bring `x` into cache". It's really "bring this many bytes from this address into the cache" - more specifically, bring in a **cache block**

If the caches in the system is empty, the directory is empty. Once a block that contains `x` is moved to cache 0, or cache 1, or both, it will be updated in the directory (a table).

- A (hardware) data structure called a **directory** that *stores the status of each cache line/block*
- When a variable is updated, the directory is *consulted*, and the cache controllers of teh cores that hvae that variable's cache line in their caches are invalidated

*essentially kinda like a metadata table that keeps track of which cache has what block of memory*

## Cache Coherence Protocols
![[cache coherence protocols.png]]

Summary:
- Snooping: everyone knows everything that happens on the bus. Hence whenever something is updated each core knows what happens
- Directory-based: a piece of hardware, it has a list of which hardware has a piece of cache block. So whenever global variable is udpated it also goges to the directory and goes to the right caches to update other caches
directory-based is more scalable so is more widely used


There are **several coherence protocols**
- MESI
- MSI
- MOESI
- ...

### MESI Protocol
![[MESI Protocol.png]]
The protocol can be applied *snoopy* or *directory* based pov, but we will consider it from a directory pov since snoopy is not used anywhere.

Any block inside a cache will always be in one of the four states below:
- modified: 
- shared: 
- invalid: 
- exclusive: i am the only one who has the copy
Almost most of the arrows have two letters. The second letter is always `W` or `R` (read or write), the first letter is always `P` or `B` (Processor or Bus) (if a cache is initiated by me, it will be `P` - I THINK)

Suppose we are in **exclusive** state (I have a copy of the cache block). I decided to write to a cache block - so we are now **modified** state. If we keep writing to that same block, we are still in that same cache block

suppose another block on the core wants to read `x`. In order to for somebody else to read it, that means it needs to be updated in the directory.
- now, they will get a cache miss, so either they will get the block from me or get memory from the other cache depending on the design. *Now, both caches will have a copy* - now it moves from **modified** to **shared**.
- If somebody else keeps reading from it, it's still shared

Now, what if somebody else has a copy of the block and needs to write to that block. In order for that to happen, i must *invalidate my copy* so that the other person has *exclusive* ownership
- you will not be able to write to a block unless you have *exclusive* ownership

**Modified** to **Invalid**:
- to get to modified we basically write first (exclusive -> modified)
- But if somebody else needs to write to it, they will get a cache miss, and they will get *exclusive* ownership of the block, and so i have to invalidate mine.

> [!Question]
> Is the states for each cache? like each cache will have a different state for a cache block
> 
> **ANSWER**: The state is for *every cache block for every cache* - e.g. a cache block can be invalid in one cache and shared in another one.
> 
> *Every cache block in every cache has its own MESI graph*


> [!question] (double check if true)
> An invalid cache remains invalid when it observes a bus read
> As long as i dont want to read or write to my cache block, it will remain invalid. i do not care about what other poeple need for the block. But as long as I decided that i don't need it (i.e. using P) i will remain invalid.


> [!Question] Bit Associativity
> Wtf is that


**PR/~S**: suppose i need to read or write `x`.
- for read `x`, if nobody has a copy of `x` cache block and i also do not, then i will have exclusive ownership
	- if i do not have it, but someone else has it, then when i read `x` it will become shared
	- Hence the ~S: means *nobody else has a copy of that block* (not shared) - hence Invalid -> Exclusive happens when its PR/~S (i wanna read it and nobody else has a copy of that blocked)

## Examples from Real Life
Multicore exampels

**Intel Cascade Lake** (architecture name; market name: Intel Xeon)
- 14 nm technology (note: latest technology in product is 3 nm)
- L1D Cache (Level 1 data cache)
	- 32 KB/core, ==8-way set associative== (every set has 8 blocks), 64 sets, 64 B line size, write-back policy (write back only when block is kicked out of set, unlike write through - if i modify my cache, i will also modify the lower level caches)
	- 1 cycle
- L2 Cache
	- Private (every core has its own)
	- 1 MB/core, 16-way set associative, 64B line size, write-back policy
	- 14 cycles latency - takes 14 cycles to see if we have a cache miss
- L3 Cache
	- 1.375 MB/core - each core gets a piece of L3 cache, and these pieces are all connected to each other (so they are still shared). this is better than just a giant L3 cache because a big one is power hungry
	- 11-way set associative
	- shared across all cores
	- 50-70 cycles latency  - slowest one, but biggest one in size so we can have a high hit rate. The bigger the cache, the higher the probability we have a hit rate

**Super Computer Example: Frontier**
- 606,208 cores
- 37,888 GPUs
- 74 cabinets (literally a cabinet that stores stuff), each cabinet has 64 blades, each blade has 2 nodes, each node = 1 CPU, 4 GPUs, and 4 TB of flash memory (per motherboard)
- This is the first machine that crossed the exascale ($10^{18}$) floating point (this is important cuz u can start analyzing human genome quickly and personalized drugs etc. precision medicine, ppl think is necessary to cure cancer)

## Conclusion
The trend now is:
- more cores per chip
- more heterogeneity
- non-bus interconnect
- NUMA and NUCA (Non-Uniform Memory/Cache Access)

**Communication and Memory access are the two most expensive operations, NOT computations**

thats it for hardware. starting next class we start talking about parallel software.