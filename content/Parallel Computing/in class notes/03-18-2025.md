## Derived Datatypes
Until now, all the stuff we have been sending are arrays - string (array of char), array of int, etc. all *elements of the same type*

what if we want to send elements from process x to process y, but elements of different types.
RIght now, from what we know, we have to do this with three different commands.

WIth derived datatypes:
- Used to represent any **collection of data items**
> a collection of items of different type is called **struct**
- If a function that sends data knows this information about the collection of data items, it can collect the items from memory before they are sent.
> if the function that sends the struct from one to another knows the information of the struct (e.g. we want to send an int from this memory), it will go and collect the info from the memory, pack them together, and send it to the destination.
- A function that receives data can distribute the items into their correct destinations in memory when they're received.

Steps:
1. MPI_Datatype *somename*;
> you make to make this declaration
2. MPI_Type create_struct()
> tell the MPI Runtime to tell this new structure that it contains this item blah blah blah.
3. MPI_Type_commit()
> now, you have a type called someone - its a new type you created. at its core its nothing but a structure. 
4. When done: MPI_Type_free();
> tell MPI Runtime you will no longer use that newly created type. this is needed because if you leave MPIRuntime with many newly created items, you will make it a little bit slower. Ofc, MPI Runtime affects performance of code

Lets look at them in detail/in action

### Derived Datatype
A structure: A sequence of basic **MPI datatypes** together with a **displacement** for each datatypes
![[MPI struct derived datatype.png]]

> Q: why do sometimes we need to put them in a structure instead of putting them separately? (in general, not related to MPI)
> A: it is there for sequence of memory. suppose you have three lines that declare int then float then char or something. even if they are in the same place in teh code there is no guarantee they are in sequence in memory. struct makes them enxt to each other and thus makes it memory friendly

displacement: *displacement from the first variable*
- 0: 24-24=0
- 16: 40-26=16
- 24: 48-24=24

### MPI_Type create_struct

Builds a derived datatype that consists of individual elements that have different basic types
![[MPI_Type_create_struct.png]]

> before you do this, oyu need to call `MPI_Datatype somename`

> [!danger] rewatch this part or review txtbook

array argument 2, 3, 4 are arrays. the number of elements in each array is represented by the first argument, `count`
- `array_of_block_lengths[]`:  ==im not sure==
> allow oyu to form a structure that contains not only scalar element by also arrays
- `array_of_displacements`: ==im not sure==
	- the type is `MPI_Aint`: MPI Address Int. it is a way for MPI to specify the address. it is a type big enough to have an address
- `array_of_types`: ==im not sure=

last argument: `new_type_p` is the ==im not sure==

![[MPI_Get_address.png]]

How to get address of `x`?
usually, you'd use `&` to get an address of a variable.  -> `&x`

How to get address of `A` with 10 elements?
1. just `A` - its a pointer
2. `&A[0]` - memory location of the first element

MPI it is a bit different. 
already `&` gets the address, and it is true usually 99% of the case itll work. the surprise is that `&` in C is not part of the industry standard - it doe snot require the `&` return the value of the pointer. Also, in some memory management tool it may not be correct.
in order to be on the safe side, just use `MPI_Get_address`

`MPI_Aint`: an integer type that is big enough to store an address on the system

---
Once this function is called, the last step before you use this structure is to run:
```C
MPI_Type_commit(MPI_Datatype* new_mpi_t_p // in/out)
```

Allows the MPI implementation to optmize its interal representation of the datatype for use in communication datatypes

Once you are done with using this datatype, better to free it to free any additional storage
```C
MPI_Type_free (...)
```

### Example
![[mpi build_mpi_type example.png]]

we now the displacement of `a_p` to `a_p` is 0, so we already have 0. we need to calculate the rest...

![[mpi built_mpi_type example 2.png]]

Now, we can use this type. for example, the `Get_input` example we saw repeatedly earlier w different versions
![[MPI Get_input example with MPI derived type.png]]

Here, `MPI_Bcast` just sends address of `a`. why is that enough? because we have built the type and we have told MPI the displacement of all the other variables to `a`.

The other nonzero processes (the receiving end) will receive the complex data as if it is a complex structure. 
- i THINK `a->x` or `a.x` erm double check this tho

## Measuring Time in MPI

this is a little bit tricky..

From the past we have:
- `time` in Linux
> we get real, sys, and usr for each of the n processes. its useful but its not what we want - we want the time for the whole process.
- `clock()` inside code
> clock gives you CPU time. you run it before and after the piece of code you want to measure, subtract, and divide by cycles per second.

These are here for traditional sequential code.

MPI offers something else

**Elapsed parallel time**: returns the number of seconds that have elapsed since somet ime in the past.
- `MPI_Wtime`: `W` stands for World time. returns double precision of some time in the past (which we dont rlly care we just care abt finish - start). kinda same as with clock we just call when the piece of code starts and when it ends.

![[MPI_Wtime.png]]
This meaures the elapsed tiem for the **calling process**
*still, we have similar problem - we are getting this number for each one of the 5 processes?*
- every process returns a elapsed time. we want to take the **maximum**. this is a **reduction** operation!
- we need to make sure every process reach the `start=MPI_Wtime()` at the same time. how to do this? SYNC PROCESSES

IMPORTANT:
- `MPI_Wtime()` returns wall clock time; so includes idle time
- `clock()` returns CPU time

### How to Sync Processes

`MPI_Barrier`: ensures that no process wil return from calling it until every process in the communicator has started calling it.

```C
int MPI_Barrier(MPI_Comm comm // in)
```

It blocks all processes until all processes has started calling it.

![[mpi barrier to measure time.png]]

- calls `MPI_Barrier` to sync them
- calls `MPI_Wtime`
- calculates `local_elapsed`
- does `MPI_Reduce` to get the maximum

### Performance of Serial vs Paralle matrixv ector multiplication
Run-times of serial and parallel matrix-vector multiplication
![[Run-times of serial and parallel matrix-vector multiplication.png]]

for smallest problem size, with one communciator we have 4 seconds. From 1 -> 2 the time almost double! 
> what hapepned to amdahl's law? its because this code only measures the parallel part (cuz measures piece of code)

but from 2->4 the increase is not as big
From 8->16 we almost don't see any speedup, *unlike the big problem size!*

with big problem size, we we increase comm_sz the time drops a lot bigger!

Speed up of ...
![[speedup of serial and parallel matrix-vector multiplication.png]]

Efficiencies of...
![[efficiencies of serial and parallel matrix-vector multiplication.png]]

Now, we are ready to go to part 4, or the last part of MPI.

## Conclusion:
- reducing messages sent is a good performance strategy!
	- collective vs point-to-potin
- distributing a fixed amount of data among several messages is more expensive than sending a single big message

# MPI Part 4: "Last Touch"

## A Parallel Sorting Algorithm

For sequential execution, you are picking the one with the best Big O notation. That is no longer the case for parallel ones - there are much more important stuff (communication, memory access)

lets try to parallelize a usually pretty bad sorting algorithm: Bubble sort

### Sorting
- `n` keys and `p=comm sz processes`
- `n/p` keys assigned to each process
> how to divide keys across processes? he mentions something abt "scatter"
- no restrictions on which keys are assigned to which processes
- when the algorithm terminates,
	- the key assigned to each process should be sorted in (say) increasing order
	- if `0 ≤ q < r < p`, then each keya ssigned to process q should be less than or equal to ==incomplete==

serial bubble sort:
![[serial bubble sort.png]]

what bubble sort: starts from beginning element 0, cmopares element 0 and 1 and swaps them only fi element 0 is bigger than element 1.

on average it will take `n` passes, each pass goes through `n` elements or something so O(n^2)

the problem with bubble sort (aside with O(n^2))
- we cannot execute the compare-swap out of order!
> this is a problem because if comparison must be done in order we do not have a chance to parallelize it
> 1 and 2, then 2 and 3, then 3 and 4, ...
- can we decouple that? (make it more parallelizable)

what if we compare the following swaps in parallel?
0th round: 0 and 1,  2 and 3, 4 and 5,  6 and 7 ...
1st round: 1 and 2, 3 and 4, 5 and 6, 7 and 8...
2nd round: 0 and 1, 2 and 3, 4 and 5, 6 and 7...
*we alternate from starting odd or even*

### Odd-even transposition sort

A sequence of phases.

Even phases, compare swaps:
`(a[0], a[1]), (a[2], a[3]), ...`

odd phases, compare swaps:
...

#### Example

Start: 5, 9, 4, 3
Even phase: compare-swap (5, 9) and (4, 3) and get the list 5, 9, 3, 4
Odd phase: compare-swap (9, 3), and get the list 5, 3, 9, 4
Even phase: compare-swap (5, 3) and (9, 4), and get the list 3, 5, 4, 9
Odd phase: compare-swap (5, 4) and get the list 3, 4, 5, 9
done

Still O(n^2) but more parallelizable.

> Quick sort is parallelizable but is not easily parallelizable. the first parallized code for quick sort is a **published research paper** so imagine how hard that is

#### Serial Odd-even transposition sort

![[odd-even transposition sort (serial).png]]

There is a lot of communication here:
Communications among tasks in odd-even sort
![[communications among tasks in odd-even sort.png]]

How do we reduce the communication?
A: recall from earlier, **Every process will be assigned n/p keys**

### Parallel Odd-even transposition sort

![[odd-even transposition sort (parallel) idea.png]]

every process will sort its own elements locally.  here, you can use any sequential sorting algorithm

After the local sort (so every 1 still have its own list), phase 0 will change a little bit of its definition of comparison swap: here, we talk at the processes level instead of the number levels. we comparison swap processes 0 and 1, processes 2 and 3.
- this means, process 1 will send its list to process 0, and process 0 will send its list to process 1. process 0 and 1 will both have 8 elements in this example
- process 0 will delete the highest four elements, process 1 will delete the lowest 4 elements (see, between time "after local sort" and time "after phase 0")
- at phase 1, which is an odd phase, the things will happen between 1 and 2. 


Pseudo Code:
![[odd-even transposition sort (parallel) pseudo ocode.png]]

> Q: if i am process 1, who is my partner at phase 0: 
> A: process 0
> Q: if i am process1, who is my partner at phase 1:
> A: process 2

`Compute_partner()`
![[odd-even Compute_partner.png]]

`MPI_PROC_NULL`: Constant defined by MPI. when used as source/destination in point-to-point comm, no comm will take place. just makes code easier.

### Safety in MPI Programs
The MPI Standard allows `MPI_Send` to behave in two different ways:
- it can simply copy the mssage into an MPI managed buffer and return
> it means it can be nonblocking
- or it can block until the matching call to `MPI_Recv` starts

Many implemenetations of MPI set a threshold at which the system switches from buffering to blocking
- relatively small messags will be buffered by MPI_Send (by the run time, which means i am nonblocking)
- larger messages will cause it to block

If the MPI_Send executed by aech process blocks, no process will be able to start executing a call to `MPI_Recv` and the program will be hung or deadlock

Each process is blocked waiting for an event that will never happen

> going back to the pseudocode, in the "Send my keys" and "Receive my keys" line: me and my partner will send and receive from each.
> if MPI_Send blocks, then i will be blocked while sending,a nd you will be blocked while sending. but now we are both blocked while sending, and none of us will reach receive
> If MPI Implementation is noblocking with send, this will work. if it is blocking, the pseudocode will work. here, you are under themercy of the version of MPI we are running under the machine!

A program that relies on MPI provided by buffering is said to be **unsafe**. Such a program may run without problems for various sets of input, but it may hang or crash with other sets.
> there is nothign in the coding that is called a "small bug" or a "big bug" - if there is some code you are using in the operation and then it crashed, will you care if the bug is because you forgot to init a variable or because of somethin gbi? no...
> hw tests theoretical knowledge, lab tests implementatino. hence he doesnt care abt "small or trivial bug". Small and trivial bug are one of the main reason that put millions of ppl out of electricity some years ago - there are huge consequences.

> Your code must not be unsafe. this means it may work sometimes but crash other times. 

Solution:
- one partner sends first then receives
- the other partner receives first then send

this solves the problem for what we have seen so far. But, is there a general way for finding out if our code is safe or unsafe? in case we found it unsafe, how do we make it safe? --> discuss next class.