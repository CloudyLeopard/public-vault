
Goal: try to enhance performance of histogram

Q: Shall we make each thread responsible for one or more character? or divide string into pieces?
A: a good rule of thumb for PC is, if problem size gets bigger, then we can add more threads and not add more work to the available thread. 
The first strategy is not good because if the string gets longer, each thread still has to read through all the string and there is more work for each thread. we also cannot add more thread.

We saw that that on a quad core, the sequential version takes 10.36 seconds (or something), and we try to parallelize with different ways. in the last lecture, we reached 3.8 seconds.

Now, let's do another version (ss alr in previous lecture)
![[openmp ascii histogram v2.1.png]]
in line 2, we create the threads (quad core, we didn't specify number of threads -> we create 4 threads).
Then each thread gets its unique id (x thread means a unique id between 0 and x-1, very similar to MPI)
In line 6, we have `#pragma omp for`. We do not have the word `parallel`: we are not creating any threads. the threads that have already been created in line number 2 will be alive in line number 3 until line 17. In line 6, we are telling openmp runtime to use the threads available right now, and distribute the following for loop iteration among them.

So, whenever we use the clause `for`, the following line must be a `for` loop. so, the number of iterations here is really related to the number of characters in the string. the number of characters will be divided among the threads - corresponds to the strategy we picked above (every thread is responsible for some part of the string)

Thread will be doing the for loop stuff inside in paralle: it will read a character, then go to its unique `tid` in the 2d array `local_histogram`. 

Here there are no critical section: no two threads will have the same `tid`. even if two threads read the same character, because they will have different `tid`, we do not have any critical section.
Also, the `for` puts **implicit barrier** at the end of the for loop at line 10. That means, no thread will be able to proceed to line 11 or 12 until all the other threads have proceeded to line 10.

At line 10, all threads will reach that point and all threads are done with their local histogram. the next step is we need to add or accumulate the local histogram.

We need two nested loop. One will go through the `tid`, then for each id we have to go through all the histogram of that id. The first outer loop will go through that `tid`, then for each `tid` we scan all the local histogram.
Here, we have `#pragma omp single`, means the following structured code (line 13 -> 15) will be *executed only once*. This is not `critical` or anything (critical means executed by all the threads but only one at a time). `single` implies only one thread eecutes the code *once*. 

If we remove the implicit barrie rin line 10, one thread may proceed before others are finished into line 12, with the threat that some threads are still working on their local results even after a thread starts accumulating stuff together.

Q: With four cores, we get **4.42** seconds -> slower than the previous one. Why is it worse? If you look at this version, why is this version slower than the previous version?

Let's look a little bit closer. starting from line 12, everything is done purely sequentially. All the other threads are staying idle.
*In the previous example*, we removed the implicit barrier. How do we guarantee teh code will be correct? in line 11, any thread that finishes will go proceed to line 11. Any thread has a unique `tid`, so whatever thread reaches that point will execute the whole for loop, and will use the `tid` and the character (represented by `i`) to add to the global histogram. But then, we may risk more than one thread reach the for loop (same `i` at teh same time), this is why we make it `atomic`. But, even if there is `atomic`, does not mean that things will be purely sequential: maybe threads are working on two different `i`'s --> parallelism.

Because of this, there is this small difference in speed.

Can we try to enhance this a little bit more? 

## Another another example
![[histogram example another another.png]]

in line 12, the outer for loop the is the `num_buckets`, and the second one is the `num_threads`. And, we are parallelizing the outer loop (`#pragma omp for`). One thread is responsible for bucket 0, 1, another thread for bucket 2, 3, and so on.

Let's look at thread 0, suppose it is responsible for bucket 0, 1, 2. It will go to 0, it will execute the whole inner for loop (`i=0`), but `t` will go over all the ids in the thread: thread 0 is responible for adding the part of the histogram for character number `i=0` for *all the threads*. Every thread parallelized here will have a different `i`, and every thread will go through *all the `t`*. No two thread will be adding to the same `i` at the same timme. This is why we can parallelize this

We cannot put a `no wait` at line 10 here ofc cuz we cannot start accumulating until we are sure all the threads are done with the local histograms.

What about the speed? **3.60 seconds**. This is better than the previous two - the best we can get from histogram.

### Lesson from this Example

- Atomic operation: sometimes we need to use it
	- They are expensive
	- Yet, they are fundamental building blocks
> fundamental: it is needed for correctness. Sometimes, if we do not have (or language does not give us `atomic` or `critical`) it, we cannot gaurantee correct code

- synchronization (the `no wait`)
	- correctness vs performance loss
	- Rich interaction of hardware-software tradeoffs
> Want speedup, but want code to be correct. Have to be very careful about the critical section. If you think about these are the two main things in the histogram.

First we brainstorm to find the best parallel strategy, then we start optimizing, and think about atomic operation and synchronization

## OpenMP Parallel Programming Model

1. Start with a *parallelizable* algorithm
	- loop level parallelism is necessary
> if you are trying to solve a problem with OpenMP, think of algorithms that have for loops that are independent. the openmp compiler will not check for correctness. It will blindly parallelize for loop even if it is wrong. 

2. Implement serially
> start with serial stuff. this is so that after step 3, we eliminate all bugs from the sequential version

3. Test and Debug

4. Annotate the code with parallelization (and synchronization) directives
	- Hope for linear speedup

5. Test and Debug
> Test for performance and debug parallel bugs. E.g. we removed a `no wait` but is now wrong.

Test and debug first with one thread: remember that one thread will be little but slower than purely sequential. One thread has a few more lines on using openmp library.

When we reach step 5, test with one thread first. If it is correct, move to two threads only. Then, 3 and 4 threads. If after 4 threads everything is ok for correctness, then go back to 2 threads and start optimizing the code for speedup.

Of course, your whole progam may have some part that can be parallelized with 3 threads then sequential then another part w 4 threads and again. so it depends on how youa re designing your code

**OpenMP uses the fork-join model of parallel execution**
![[openmp programming thread flow.png]]

When you start your openmp program, it will start with one single thread: a **master thread** with `ID=0`. Then, the master thread will **fork** then creates a team of parallel threads. Once we are outside of the `pragma omp parallel` structured piece of code, we are back to initial thread with ID=0. **join**

**Forking** and **join**

### Programming Model - Threading
Basically the above diagram but in code

![[parallel threading flow code.png]]

## What we have learned so far

- `#include <omp.h>`
- `gcc -fopenmp ...`
- `omp_set_num_threads(x);`
> We saw earlier that openmp setes number of threads. this is an api that *does not create threads*. it just tells openmp run time. Whenever in future he asks us to create thread he wants the thread to be in existence
- `omp_get_thread_num()`
> it will return a different number for each calling thread. this is the **unique id** starting from 0
- `omp_get_num_threads()`
> this will tell you the total number of threads
- `#pragma omp parallell [num_threads(x)]`
> if both `omp_set_num_threads` is there and `num_threads` is here, `num_threads` will take precedence
- `#pragma omp critical`
> also `atomic`. We will discuss the difference next week.

## Conclusion
End of first part of OpenMP

OpenMP is a standard for programming shared-memory systems. It will continue to gain popularity - because now it can also send work to GPUs. It can also send work to accelerators. It is becoming more versatile.

OpenMP uses both API functions and preprocesssor directives called pragmas.

OpenMP programs start multiple threads rather than multiple processes
> we are talking about **one process**, **multiple threads**. This means code for openmp must run on a shared memory machine (one mother board)

# OpenMP II

## Scope

in serial programming, the scope of a variable consists of those parts of a program in which the variable can be used
> we know local variables are only used in a functino they are defined; global variables can be used in any program in the current C file, and so on

In parallel, the definition of scope is a little bit different

In OpenMP, the scope of a variable refers to the **set of threads that can access the variable in a parallel block**
> if there are variables inside parallel block, that variable can be acess by all the threads (**shared variable**), or every thread will have its own copy of that thing (e.g. histogram version of `tid` - private to each thread)
> this is under your control (as the programer). You can choose which variable is private to every thread, and which variable must be shraed by allt he thread.
> must be careful: for shared variable, if more than one thread is updating that variable, it is a critical section and have to be careful of what you are doing.


### Scope in Open MP

A variable that can be access by all the threads in the team has **shared** scope.
> if we have `#pragma omp parallel`, if we have `int x` defined *before the parallel block*, it is **shared**. `x` can be used my all the parallel section/threads.

A variable that can only be accessed by a single thread has **private** scope.
> variables declared inside a parallel bock is private. every thread has its own `x` if `int x` is declared after the `#pragma omp parallel` line.

The default scope for variables declared before a parallel block is shared.

![[scope openmp example.png]]

`thread_count` is declared *before* `#pragma omp` - all the threads can access the same variable `thread_count`. 

All the threads will access the function `Hello`
Inside `Hello`, we have `my_rank` a variable declared `Hello` which is inside a parallel block. So, `my_rank` is private - every thread has its own `my_rank`. Every thread will get a unique id.

Here, `thread_count` inside `Hello` is *different* from the one outside. This will be the total number of threads created, which is inside `Hello`, which is different from the parallel version (it is private). Even though `omp_get_num_threads` will return the same number, it si *different*.

Q: From programmer perspective, why did we use `thread_count` here? in `Hello`. 
Answer: related to grammar of C. This is because `thread_count` is not global! Inside `Hello`, we cannot access `thread_count` defined in `main()`. So, we need to have something.

The original rules of **Scope** still applies!

### Back to the Trapezoidal Example
see `Trap` function from [[04-01-2025]]
![[openmp trap code.png]]

In order to add `my_result`, every thread has its own `my_result`, which is the sum of the local trap. 
`global_result` is critical section.

How about something like this instead:
```C
double Local_trap(double a, double b, int n);
```
and we use it like this:
```C
global_result = 0.0;
# pragma omp parallel num_threads(thread_count)
{
	# pragma omp critical
	global_result += Local_trap(double a, double b, int n);
}
```

`global_result` is declared before `# pragma omp parallel` so it is a shared variable.

Q: first of all, is this code *correct*?
A: yes, it is correct

Q: Performance?
A: The performance is *extremely bad*. It is much worse than the sequential code. This is because you created the threads, then you put the call to this function critical section - one thread at a time will execute `Local_trap`. There is no parallelism whatsoever here

We force the threads to execute sequentially. It is now slower than a version with single thread!

We can fix this problem by:
1. declare a prviate variable inside the parallel block
2. Moving the critical section after the function call

```C
global_result = 0.0;
# pragma omp parallel num_threads(thread_count)
{
	double my_result = 0.0; /* private */
	my_result += Local_trap(double a, double b, int n);
	
	# pragma omp critical
	global_result += my_result;
}
```
The trapezoids are calculated in parallel

Can we do better? --> YES!

but, we first need to learn the **Reduction Operators**

## Reduction Operators

For reduction in MPI, we need to call an API

A **reduction** is a computation that repeatedly applies the same reduction **operator** to a sequence of operands to get a single result stord in **reduction variable** (We have seen that in MPI!)
> reduction variable: the variable that stores the final result


A reduction clause can be added to a parallel directive (`# pragma omp ...`)
```C
reduction (<operator>: <variable list>)
```
Operators include: `+, *, -, &, |, ^, &&, ||`
Variable list: can be a comma separated list

The code becomes:
```C
global_result = 0.0;
# pragma omp parallel num_threads(thread_count) \
		reduction(+: global_result)
	global_result += Local_trap(double a, double b, int n);
```

No longer need critical section.
The result will be in `global_result` which is shared

We add the `\` to make sure that this is all in the same line. the `\` is a way to tell the OpenMP preprocessor is continuing the next line

Here, this local trapezoid will be executed in parallel in different threads - no critical section. And then they will add the local results to `global_result`. OpenMP will make sure that the addition is correct - it make sure everything is run in parallel and only force sequential stuff at the end when its adding to `global_result`

> [!Question]
> Is it the same performance as the code without reduction operator?
> Like, is the only thing it does to make the code look cleaner?

## `#pragma omp parallel for`

Note: `parallel` is the keyword for creating threads.

The following line after `for` *must* be for loop.

1. Forks a team of threads to execute the following structured block.
2. The structured block following the parallel for directive **must be a for loop**
3. The system parallelizes the for loop by **dividing the iterations of the loop among the threads**

Now, can this `#pragma omp parallel for` help us with trapezoidal stuff even in an easier way?

![[openmp trapzoidal with for.png]]
top left is the sequential code

Bottom: how about we have a for loop
`num_threads` is whatever the user enters, we enter everything into `approx` (the results will be in it). Once we are done multiplying by `h` is back to sequential. This will also tell us something else: the code is correct, and the third rule for openmp

Rules:
1. variables declared before parallel block is shared
2. variables inside parallel block is private
3. ==whenever there is parallel for, the loop index is **private**==
Every thread will be taking consecutive iterations - it will be using `i` inside the loop body so the `i` cannot be shared.

## "Scope" Rules
- the default scope for variables declared before a parallel lbock is shared
- the default scope for variables declared inside a parallel block is private
- the default scope of loop index is prviate

We have been using the word "default", means that if we do nothing this will be the results that apply, but this also means these rules **can be overriden**

- You can override the above rules using `default()`, `shared()`, and `private()` clauses
> clauses means you can use them after the line `# pragma omp ...`

OpenMP is designed to parallelize for loops. Parallelizing for loops is your job to ensure the loop iterations are independent.

### Legal Forms for Parallelizable For Statements
The for loop must be in one of these forms:

![[Legal Forms for Parallelizable For Statements.png]]

Number of iterations MUST be known prior to loop execution. Not at compiling time, or loading time, but *before the for loop*.
OpenMP won't parallelize while loops or do-while loops.

#### Caveats

- The variable **index** must have integer or pointer type (e.g. it can't be a float)
> There isn't rlly a language that allows for floating point index. Why?
> The catch: at the end, it is a floating point, and you are incrementing it, and there may be some approxiation error with floating point. Based on precision of the machine, the number if iterations may differ. We do not want to enter into that thing, so loop index must be int or pointer type.
>
> Pointer is an address

- The expressions **start**, **end**, and **incr** must have a compatible type.
> basically you cannot mix type

- The expressions **start**, **end**, and **incr** must not change during execution of the loop
> changing them may change number of iteration, which is not allowed in OpenMP

- During execution fo the loop, the variable **index** can only be modified by the "increment expression" in the for statement
> You cannot modify `index` inside the loop body

### Data Dependencies
Example: fibonacci sequence

![[openmp data dependencies example fibonacci.png]]

Code sometimes is correct sometimes is wrong

1. OpenMP compilers dont' check for dependencies among iterations in a loop that's being parallelized with a parallel for directive
> it is very hard by static analysis alone to check for dependencies between loop iterations. So, we let the programmer decide that the loop is independent and compiler parallelize them blindly

2. A loop in which the results of one or mor eiterations depend on other iterations cannot, in general, be correctly parallelized by OpenMP

#### Example 1:

Q: Do we have to worry about the following?
```C
# pragma omp parallel for num_threads(2)
for (i = 0; i < n; i++) {
	x[i] = a + i*h;
	y[i] = exp(x[i]);
}
```

Two threads, every thread will take `n/2` iterations. At every iteration, the thread will execute the first line, then the second line. The two lines will never be executed out of order, so we will be ok.

Also, in the loop body, the `i` does not get modified. the iteration here are independent and are correct.

Let's see a more sophisticated example:

#### Example 2: Estimating Pi
![[estimating pi sequential.png]]

We alternate between positive and negative, and denominators are odd numbers. At the end, we multiply everything by 4.

We can write the sequential code as 
```C
double factor = 1.0;
double sum = 0.0;
for (k = 0; k < n; k++) {
	sum += factor / (2*k+1);
	factor = -factor;
}
pi_approx = 4.0*sum;
```

The higher `n`, the more accurate.

**OpenMP solution #1**

```C
double factor = 1.0;
double sum = 0.0;
# pragma omp parallel for num_threads(thread_count) \
	reduction(+:sum)
for (k = 0; k < n; k++) {
	sum += factor / (2*k+1);
	factor = -factor;
}
pi_approx = 4.0*sum;
```

Q: is this code correct?
A: this code is wrong. `factor` is declared outside of `# pragma` and what if two threads where one needs `factor` positive and the other needs `factor` negative? we have a problem. The problem is the alternation between positive and negative is not a reduction operation.

**Open MP Solution #2**

```C
double sum = 0.0;
# pragma omp parallel for num_threads(thread_count) \
	reduction(+:sum)
for (k = 0; k < n; k++) {
	if (k % 2 == 0)
		factor = 1.0;
	else
		factor = -1.0;
		
	sum += factor / (2*k+1);
}
pi_approx = 4.0*sum;
```

Q: is this code correct?
A: This code is wrong still. Because i did not change anything... i just made the code more complicated and just makes it look like its correct. What if a thread needs `factor` 1 and another needs it `-1`? I didn't solve anything

**OpenMP Solution #3**

```C
double sum = 0.0;
# pragma omp parallel for num_threads(thread_count) \
	reduction(+:sum) private(factor)
for (k = 0; k < n; k++) {
	if (k % 2 == 0)
		factor = 1.0;
	else
		factor = -1.0;
		
	sum += factor / (2*k+1);
}
pi_approx = 4.0*sum;
```

Every thread will have its own copy of `factor`. Also, you can have inside `# pragma omp parallel` the line `shared` and something that is private can be shared. There is also something called `default`

### The `default` clause

Lets the programmer specify the scope of each variable in a block
```C
default(none)
```

With this clause the compiler will require that we specify (by hand) the scope of each variable we use in the block and that has been declared outside the block.

This is extra work but its good for readability - inside of remembering the default rules, you will force by hand what is shared and what is private.

```C
double sum = 0.0;
# pragma omp parallel for num_threads(thread_count) \
	default(none) reduction(+:sum) private(k, factor) \
	shared(n)
for (k = 0; k < n; k++) {
	if (k % 2 == 0)
		factor = 1.0;
	else
		factor = -1.0;
		
	sum += factor / (2*k+1);
}
pi_approx = 4.0*sum;
```

`default(none)` removes *all* the default rules. And now you can redeclare the variables as private or shared.

This includes array! If inside for loop ther eis an array `a`, the namem of the array *must appear in the clause* `private` or `shared`. You cannot tell OpenMP to decide if you have used `default(none)` and remove all the default rules.


### Conclusions
To make the best use of OpenMP try to have programs with for-lops
> start by writing sequential programs with for loop, then inject OpenMP stuff. before this, ensure sequential code is correct.

Scope is an error prone concept here. So be careful!
- It may be a good idea to explicitly specify the scope of each variable in the parallel (or parallel for) block.
> i.e. use `default(None)`

> [!Question] What happens after the parallel block ends?
> i guess... its just one thread left... and so the parallel idea of "scope" just ends? so this is like a non question really

