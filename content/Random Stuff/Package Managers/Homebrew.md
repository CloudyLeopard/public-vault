https://brew.sh/

package installer for Apple

keeps all the installed packages inside the directory under homebrew too, so it makes managing packages really easy

for future cases, if i can install a package with homebrew, do that