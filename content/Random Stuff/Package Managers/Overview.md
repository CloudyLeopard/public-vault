from what i understand

use [[homebrew]] to manage mac's **overall** software/package installation. I used brew to install hatch, miniconda, etc.

[[Conda]]: used for managing environments and dependencies, especially for data science and scientific computing. Supports Python and non-Python dependencies

[[Hatch]]: used for managing Python environments and **packaging**, and makes Python packaging easier with `pyproject.toml`